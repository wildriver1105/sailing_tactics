# Sailing Tactics agent instructions

When a request creates, revises, validates, imports, or installs a sailing
tactics lecture, lesson, drill, race-rule scenario, animation sequence, or
Scenario JSON, read and follow
`skills/sailing-tactics-lesson-editor/SKILL.md` completely before acting.

Use the skill's validator and installation command instead of directly
appending unvalidated objects to `content/scenarios/ai-lessons.json`.
